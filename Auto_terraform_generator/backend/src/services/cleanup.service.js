// Retained for future use — currently unused.
// Field cleanup is handled by coerceValue returning undefined for empty values,
// so the generator naturally omits them.
export function removeEmptyFields(obj) { return obj; }
